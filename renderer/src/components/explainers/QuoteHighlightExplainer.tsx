import {explainerFont, progress} from './shared';
export const QuoteHighlightExplainer = ({config, frame}: {config: any; frame: number}) => <div style={{padding: '75px 110px', color: 'white', fontFamily: explainerFont}}>{config.lines.map((line: any, index: number) => <div key={`${line.text}-${index}`} style={{position: 'relative', width: 'fit-content', fontSize: 36, lineHeight: 1.65, opacity: progress(frame, index * 16)}}>{line.text}{line.highlight ? <div style={{position: 'absolute', left: 0, right: 0, bottom: 7, height: 5, borderRadius: 9, background: '#74d394', transformOrigin: 'left', transform: `scaleX(${progress(frame, index * 16 + 12, 20)})`}} /> : null}</div>)}{config.source ? <div style={{fontSize: 23, color: '#aaa', marginTop: 24}}>— {config.source}</div> : null}</div>;
