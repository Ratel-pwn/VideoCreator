import {explainerFont, progress} from './shared';
export const RelationLoopExplainer = ({config, frame}: {config: any; frame: number}) => {const count = config.nodes.length; return <div style={{position: 'relative', width: '100%', height: '100%', fontFamily: explainerFont}}><svg viewBox="0 0 1200 560" style={{position: 'absolute', inset: 0, width: '100%', height: '100%'}}>{config.nodes.map((node: any, index: number) => {const next = config.nodes[(index + 1) % count]; const a = (Math.PI * 2 * index) / count - Math.PI / 2; const b = (Math.PI * 2 * ((index + 1) % count)) / count - Math.PI / 2; return <line key={`${node.id}-${next.id}`} x1={600 + Math.cos(a) * 350} y1={280 + Math.sin(a) * 190} x2={600 + Math.cos(b) * 350} y2={280 + Math.sin(b) * 190} stroke="#74d394" strokeWidth="5" strokeDasharray="12 10" opacity={progress(frame, index * 10)} />})}</svg>{config.nodes.map((node: any, index: number) => {const a = (Math.PI * 2 * index) / count - Math.PI / 2; return <div key={node.id} style={{position: 'absolute', left: `${50 + Math.cos(a) * 30}%`, top: `${50 + Math.sin(a) * 32}%`, transform: 'translate(-50%,-50%)', padding: '18px 28px', borderRadius: 999, background: '#f4f2ec', color: '#222', fontSize: 28, fontWeight: 650, opacity: progress(frame, index * 10)}}>{node.label}</div>})}</div>};
