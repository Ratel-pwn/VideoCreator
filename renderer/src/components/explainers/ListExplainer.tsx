import {explainerFont, progress} from './shared';
export const ListExplainer = ({config, frame}: {config: any; frame: number}) => <div style={{padding: '70px 100px', color: 'white', fontFamily: explainerFont}}>{config.title ? <div style={{fontSize: 38, fontWeight: 700, marginBottom: 34}}>{config.title}</div> : null}{config.items.map((item: string, index: number) => <div key={item} style={{fontSize: 32, margin: '22px 0', opacity: progress(frame, index * 13), transform: `translateX(${(1 - progress(frame, index * 13)) * 28}px)`}}><span style={{color: '#74d394', marginRight: 18}}>●</span>{item}</div>)}</div>;
