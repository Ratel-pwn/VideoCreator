import {explainerFont, progress} from './shared';
export const FlowExplainer = ({config, frame}: {config: any; frame: number}) => <div style={{display: 'flex', height: '100%', alignItems: 'center', justifyContent: 'center', gap: 28, fontFamily: explainerFont}}>{config.nodes.map((node: any, index: number) => <div key={node.id} style={{display: 'flex', alignItems: 'center', gap: 28, opacity: progress(frame, index * 14)}}><div style={{padding: '24px 34px', borderRadius: 16, background: '#f4f2ec', color: '#222', fontSize: 31, fontWeight: 650}}>{node.label}</div>{index < config.nodes.length - 1 ? <div style={{fontSize: 40, color: '#74d394', transform: `scaleX(${progress(frame, index * 14 + 8)})`}}>→</div> : null}</div>)}</div>;
